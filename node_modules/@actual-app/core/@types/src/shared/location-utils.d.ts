/**
 * Pure utility functions for location calculations and formatting.
 * These functions have no side effects and can be easily tested.
 */
export type LocationCoordinates = {
    latitude: number;
    longitude: number;
};
/**
 * Calculate the distance between two geographic coordinates using the Haversine formula
 * @param pos1 First position coordinates
 * @param pos2 Second position coordinates
 * @returns Distance in meters
 */
export declare function calculateDistance(pos1: LocationCoordinates, pos2: LocationCoordinates): number;
/**
 * Format a distance in meters to a human-readable string
 * @param meters Distance in meters
 * @returns Formatted distance string
 */
export declare function formatDistance(meters: number): string;
//# sourceMappingURL=location-utils.d.ts.map