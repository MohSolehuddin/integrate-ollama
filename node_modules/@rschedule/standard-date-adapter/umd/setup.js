(function(global, factory) {
  typeof exports === 'object' && typeof module !== 'undefined'
    ? factory(
        require('@rschedule/core'),
        require('@rschedule/core/generators'),
        require('@rschedule/core/rules/ICAL_RULES'),
        require('@rschedule/standard-date-adapter'),
      )
    : typeof define === 'function' && define.amd
    ? define([
        '@rschedule/core',
        '@rschedule/core/generators',
        '@rschedule/core/rules/ICAL_RULES',
        '@rschedule/standard-date-adapter',
      ], factory)
    : ((global = global || self),
      factory(
        global.rSchedule,
        global.rScheduleGenerators,
        global.rScheduleRules,
        global.rScheduleStandardDateAdapter,
      ));
})(this, function(core, generators, ICAL_RULES, standardDateAdapter) {
  'use strict';

  core.DateAdapterBase.adapter = standardDateAdapter.StandardDateAdapter;
  generators.Rule.recurrenceRules = ICAL_RULES.ICAL_RULES;
});
