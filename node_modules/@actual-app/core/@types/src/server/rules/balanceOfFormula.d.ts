import type * as db from '#server/db';
/** Collect formula strings from serialized or live rule actions. */
export declare function collectFormulasFromActions(actions: Array<{
    options?: {
        formula?: string;
    };
}>): string[];
/** Decode escape sequences inside a double-quoted formula string literal. */
export declare function decodeBalanceOfQuotedLiteral(inner: string): string;
/**
 * Distinct decoded string literals from BALANCE_OF("…") calls in a formula.
 */
export declare function extractBalanceOfLiterals(formula: string): string[];
/**
 * Resolve account id: map key (id) first, else first exact name match.
 */
export declare function resolveAccountIdForBalanceOf(literal: string, accountsMap: Map<string, db.DbAccount>): string | null;
//# sourceMappingURL=balanceOfFormula.d.ts.map