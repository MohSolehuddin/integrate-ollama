import type { RuleConditionEntity } from '#types/models';
import type { ForecastResult } from '#types/models/forecast';
export type ForecastRequestParams = {
    accountIds?: string[];
    conditions?: RuleConditionEntity[];
    conditionsOp?: 'and' | 'or';
    startDate?: string;
    endDate?: string;
    includeAccountlessSchedules?: boolean;
};
export declare function generateForecast({ accountIds, conditions, conditionsOp, startDate, endDate, includeAccountlessSchedules }: ForecastRequestParams): Promise<ForecastResult>;
export type ForecastHandlers = {
    'forecast/generate': (params: {
        accountIds?: string[];
        conditions?: RuleConditionEntity[];
        conditionsOp?: 'and' | 'or';
        startDate?: string;
        endDate?: string;
        includeAccountlessSchedules?: boolean;
    }) => Promise<ForecastResult>;
};
export declare const app: {
    events: import("mitt").Emitter<{
        sync: import("../../types/server-events").ServerEvents['sync-event'];
        'load-budget': {
            id: string;
        };
    }>;
    handlers: ForecastHandlers;
    services: (() => () => void)[];
    unlistenServices: (() => void)[];
    method<Name extends string & "forecast/generate">(name: Name, func: ForecastHandlers[Name]): void;
    service(func: () => () => void): void;
    combine(...apps: any[]): void;
    startServices(): void;
    stopServices(): Promise<void>;
};
//# sourceMappingURL=app.d.ts.map