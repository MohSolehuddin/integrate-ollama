import {
  DateTime,
  IRecurrenceRule,
  IRecurrenceRulesIterator,
  RecurrenceRuleResult,
} from '@rschedule/core';
export declare abstract class RecurrenceRule<T> implements IRecurrenceRule {
  start: DateTime;
  end?: DateTime;
  options: T;
  protected processor: IRecurrenceRulesIterator<T>;
  constructor(processor: IRecurrenceRulesIterator<T>);
  abstract run(date: DateTime): RecurrenceRuleResult;
}
