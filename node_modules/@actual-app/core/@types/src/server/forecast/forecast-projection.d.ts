import type { TransactionEntity } from '#types/models';
import type { ForecastResult } from '#types/models/forecast';
import type { AccountWithComputedBalance } from './forecast-accounts';
import type { ForecastFilterInfo } from './forecast-filters';
import type { ForecastScheduleOccurrence } from './forecast-schedules';
type ScheduleOccurrenceSummary = {
    amount: number;
    payee: string;
    scheduleId: string;
    scheduleName: string;
};
type ScheduleOccurrencesByAccount = Record<string, Record<string, ScheduleOccurrenceSummary[]>>;
export type ForecastDateContext = {
    forecastStartDate: string;
    forecastEndDate: string;
    forecastDays: string[];
    firstForecastDate: string;
    endDateObj: Date;
};
type ProjectForecastDataParams = {
    accounts: AccountWithComputedBalance[];
    transactions: TransactionEntity[];
    futureOccurrences: ForecastScheduleOccurrence[];
    filterInfo: ForecastFilterInfo;
    dateContext: ForecastDateContext;
};
export declare function buildForecastDateContext(startDate: string | undefined, endDate: string | undefined): ForecastDateContext;
export declare function createEmptyForecastResult(forecastStartDate: string, forecastEndDate: string): ForecastResult;
export declare function indexScheduleOccurrences(futureOccurrences: ForecastScheduleOccurrence[], accountIdSet: Set<string>, filterInfo: ForecastFilterInfo, firstForecastDate: string, forecastEndDate: string): ScheduleOccurrencesByAccount;
export declare function projectForecastData({ accounts, transactions, futureOccurrences, filterInfo, dateContext }: ProjectForecastDataParams): Pick<ForecastResult, 'dataPoints' | 'lowestBalance'>;
export {};
//# sourceMappingURL=forecast-projection.d.ts.map